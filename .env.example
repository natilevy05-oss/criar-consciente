# Copiá este archivo como referencia y cargá los valores reales
# en Vercel → tu proyecto → Settings → Environment Variables
# (no subas nunca un .env con claves reales a GitHub)

RESEND_API_KEY=
RESEND_AUDIENCE_ID=
NOTIFY_EMAIL=
FROM_EMAIL=
